org.gradle.jvmargs=-Xmx2560m -XX:MaxMetaspaceSize=1g -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# MAPBOX_DOWNLOADS_TOKEN is deliberately NOT set here — it's a Mapbox
# *secret* token (unlike the public "pk." token used at runtime), so it
# must never be committed to git. Gradle reads it from this machine's
# global ~/.gradle/gradle.properties instead (set once, outside any repo).
# See https://docs.mapbox.com/android/maps/guides/install/ if this needs
# to be set up again on a fresh machine.